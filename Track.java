package sem01;

public class Track extends Transport{
    public Track(String color, String model, int wheelNumber, int weight, int speed) {
        super(color, model, wheelNumber, weight, speed);
    }
}
